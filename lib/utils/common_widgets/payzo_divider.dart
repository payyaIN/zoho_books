import 'package:flutter/material.dart';

class PayzoDivider extends StatelessWidget {
  const PayzoDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color.fromRGBO(228, 228, 228, 1),
      thickness: 1,
    );
  }
}
