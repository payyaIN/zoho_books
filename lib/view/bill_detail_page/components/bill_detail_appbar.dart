import 'package:payzo_books/import_data.dart';

PreferredSizeWidget billDetailAppBar({required BuildContext context}) {
  return reusableAppBar(
      title: 'Bill Details', showBackButton: true, context: context);
}
